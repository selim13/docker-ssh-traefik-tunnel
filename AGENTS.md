# SSH to Traefik Tunnel

A self-hosted SSH tunnel server that automatically exposes forwarded ports as
HTTP/HTTPS subdomains through Traefik.

## Dev rules

Lint and test after editing session (but not after every edit):

- `just lint`
- `just test`
